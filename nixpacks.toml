[phases.install]
cmds = ["pip install fastapi uvicorn python-multipart pandas openpyxl xlrd anthropic aiofiles python-dotenv numpy"]

[start]
cmd = "uvicorn backend_main:app --host 0.0.0.0 --port $PORT"
